id:
  value: "urn:uuid:94ecae35-dcfd-4182-8550-22c7164fe23f"
type:
  value: ["Announce","coar-notify:ReviewAction"]
  description: |
    The `type` of this activity MUST include the values:
    - *Announce*
    - *coar-notify:ReviewAction*
origin:
  lookup: "pci"
target:
  lookup: "repository"
actor:
  lookup: "pci"
object:
  description: >
    This is a review published by PCI. The `ietf:cite-as` is not included because the review is not intended to be directly citable (rather the endorsement should be cited).
  value:
    id: "https://evolbiol.peercommunityin.org/articles/rec?articleId=794#review-3136"
    type:
      - "Page"
      - "sorg:WebPage"
context:
  lookup: "preprint"
inReplyTo:
  lookup: "optional-inReplyTo"